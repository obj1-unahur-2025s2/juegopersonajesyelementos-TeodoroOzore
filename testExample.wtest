
import armas.*
import elementos.*
import jugadoresPersonajes.*


describe "test general" {
  test "luisa usa a floki, encuentra a aurora, la ataca y esta muere" {
    luisa.personajeActivo(floki)
    luisa.aparece(aurora)
    assert.notThat(aurora.estaViva())
  }
}